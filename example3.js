function un_defined() {
  var a;
  console.log(a);
  a = "Before assigning value if we call a variable, it shows UNDEFINED.";
  console.log(a);
}
function not_defined() {
  console.log(
    "When we did not declare the variable and try to call that variable, it shows NOT-DEFINED."
  );
  console.log(b);
}